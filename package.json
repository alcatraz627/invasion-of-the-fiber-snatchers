{
  "name": "fiber-snatcher",
  "version": "0.4.1",
  "description": "Fiber Snatcher — a local debugging toolkit that gives Claude first-class inspect/drive/assert capability on a React dev app.",
  "private": true,
  "type": "module",
  "bin": {
    "fiber-snatcher": "./bin/fiber-snatcher.ts"
  },
  "scripts": {
    "fs": "bun run bin/fiber-snatcher.ts",
    "typecheck": "bun tsc --noEmit",
    "check-versions": "bun scripts/check-versions.ts",
    "test:eval": "bun tests/eval-harness.ts",
    "prerelease": "bun scripts/check-versions.ts && bun tsc --noEmit && bun tests/eval-harness.ts",
    "install-link": "bash scripts/install.sh"
  },
  "dependencies": {
    "playwright": "1.55.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@tanstack/react-query": "^5.101.2",
    "@types/bun": "1.3.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "jotai": "^2.20.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "typescript": "5.9.3"
  }
}
